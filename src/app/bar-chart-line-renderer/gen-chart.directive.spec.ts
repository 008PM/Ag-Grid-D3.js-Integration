import { GenChartDirective } from './gen-chart.directive';

describe('GenChartDirective', () => {
  it('should create an instance', () => {
    const directive = new GenChartDirective();
    expect(directive).toBeTruthy();
  });
});
